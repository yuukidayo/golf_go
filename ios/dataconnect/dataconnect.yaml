specVersion: "v1"
serviceId: "ios"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ios-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
